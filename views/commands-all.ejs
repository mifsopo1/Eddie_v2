<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Commands - Discord Logger</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="nav-brand">
            <h2>🤖 <%= client.user.tag %></h2>
        </div>
        <div class="nav-links">
            <a href="/">Dashboard</a>
            <a href="/messages">Messages</a>
            <a href="/deleted">Deleted</a>
            <a href="/members">Members</a>
            <a href="/moderation">Moderation</a>
            <a href="/attachments">Attachments</a>
            <a href="/voice">Voice</a>
            <a href="/invites">Invites</a>
            <a href="/commands" class="active">Commands</a>
            <a href="/analytics">Analytics</a>
            <a href="/logout" class="logout">Logout</a>
        </div>
    </nav>
    
    <div class="container">
        <div class="page-header">
            <div>
                <h1>⚙️ All Commands Manager</h1>
                <p class="page-subtitle">Enable/disable all bot commands including built-in ones</p>
            </div>
            <div style="display: flex; gap: 1rem;">
                <a href="/commands" class="btn btn-secondary">← Back to Custom Commands</a>
            </div>
        </div>
        
        <!-- Fun Commands -->
        <% if (commandsByCategory.fun && commandsByCategory.fun.length > 0) { %>
        <div class="command-section">
            <div class="section-header">
                <h2>🎮 Fun Commands (<%= commandsByCategory.fun.length %>)</h2>
            </div>
            <div class="commands-list">
                <% commandsByCategory.fun.forEach(command => { %>
                    <div class="command-row <%= command.enabled ? '' : 'disabled' %>" data-category="fun">
                        <div class="command-main">
                            <div class="command-info">
                                <div class="command-name-row">
                                    <h3><%= command.name %></h3>
                                    <span class="command-badge fun">fun</span>
                                    <span class="badge <%= command.type === 'builtin' ? 'badge-builtin' : 'badge-custom' %>">
                                        <%= command.type === 'builtin' ? 'Built-in' : 'Custom' %>
                                    </span>
                                    <span class="command-status <%= command.enabled ? 'enabled' : 'disabled' %>">
                                        <%= command.enabled ? 'Enabled' : 'Disabled' %>
                                    </span>
                                </div>
                                <p class="command-desc"><%= command.description || 'No description' %></p>
                            </div>
                            <div class="command-actions-row">
                                <% if (command.type === 'custom') { %>
                                    <button class="btn-icon" onclick="window.location.href='/commands'" title="Edit in Custom Commands">
                                        ✏️
                                    </button>
                                <% } %>
                                <button class="btn-icon toggle" 
                                        onclick="toggleCommand('<%= command.type %>', '<%= command.type === 'custom' ? command._id : command.name %>')" 
                                        title="Toggle">
                                    <%= command.enabled ? '👁️' : '👁️‍🗨️' %>
                                </button>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        </div>
        <% } %>
        
        <!-- Info Commands -->
        <% if (commandsByCategory.info && commandsByCategory.info.length > 0) { %>
        <div class="command-section">
            <div class="section-header">
                <h2>ℹ️ Info Commands (<%= commandsByCategory.info.length %>)</h2>
            </div>
            <div class="commands-list">
                <% commandsByCategory.info.forEach(command => { %>
                    <div class="command-row <%= command.enabled ? '' : 'disabled' %>" data-category="info">
                        <div class="command-main">
                            <div class="command-info">
                                <div class="command-name-row">
                                    <h3><%= command.name %></h3>
                                    <span class="command-badge info">info</span>
                                    <span class="badge <%= command.type === 'builtin' ? 'badge-builtin' : 'badge-custom' %>">
                                        <%= command.type === 'builtin' ? 'Built-in' : 'Custom' %>
                                    </span>
                                    <span class="command-status <%= command.enabled ? 'enabled' : 'disabled' %>">
                                        <%= command.enabled ? 'Enabled' : 'Disabled' %>
                                    </span>
                                </div>
                                <p class="command-desc"><%= command.description || 'No description' %></p>
                            </div>
                            <div class="command-actions-row">
                                <% if (command.type === 'custom') { %>
                                    <button class="btn-icon" onclick="window.location.href='/commands'" title="Edit in Custom Commands">
                                        ✏️
                                    </button>
                                <% } %>
                                <button class="btn-icon toggle" 
                                        onclick="toggleCommand('<%= command.type %>', '<%= command.type === 'custom' ? command._id : command.name %>')" 
                                        title="Toggle">
                                    <%= command.enabled ? '👁️' : '👁️‍🗨️' %>
                                </button>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        </div>
        <% } %>
        
        <!-- Utility Commands -->
        <% if (commandsByCategory.utility && commandsByCategory.utility.length > 0) { %>
        <div class="command-section">
            <div class="section-header">
                <h2>🔧 Utility Commands (<%= commandsByCategory.utility.length %>)</h2>
            </div>
            <div class="commands-list">
                <% commandsByCategory.utility.forEach(command => { %>
                    <div class="command-row <%= command.enabled ? '' : 'disabled' %>" data-category="utility">
                        <div class="command-main">
                            <div class="command-info">
                                <div class="command-name-row">
                                    <h3><%= command.name %></h3>
                                    <span class="command-badge utility">utility</span>
                                    <span class="badge <%= command.type === 'builtin' ? 'badge-builtin' : 'badge-custom' %>">
                                        <%= command.type === 'builtin' ? 'Built-in' : 'Custom' %>
                                    </span>
                                    <span class="command-status <%= command.enabled ? 'enabled' : 'disabled' %>">
                                        <%= command.enabled ? 'Enabled' : 'Disabled' %>
                                    </span>
                                </div>
                                <p class="command-desc"><%= command.description || 'No description' %></p>
                            </div>
                            <div class="command-actions-row">
                                <% if (command.type === 'custom') { %>
                                    <button class="btn-icon" onclick="window.location.href='/commands'" title="Edit in Custom Commands">
                                        ✏️
                                    </button>
                                <% } %>
                                <button class="btn-icon toggle" 
                                        onclick="toggleCommand('<%= command.type %>', '<%= command.type === 'custom' ? command._id : command.name %>')" 
                                        title="Toggle">
                                    <%= command.enabled ? '👁️' : '👁️‍🗨️' %>
                                </button>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        </div>
        <% } %>
        
        <!-- Moderation Commands -->
        <% if (commandsByCategory.moderation && commandsByCategory.moderation.length > 0) { %>
        <div class="command-section">
            <div class="section-header">
                <h2>🔨 Moderation Commands (<%= commandsByCategory.moderation.length %>)</h2>
            </div>
            <div class="commands-list">
                <% commandsByCategory.moderation.forEach(command => { %>
                    <div class="command-row <%= command.enabled ? '' : 'disabled' %>" data-category="moderation">
                        <div class="command-main">
                            <div class="command-info">
                                <div class="command-name-row">
                                    <h3><%= command.name %></h3>
                                    <span class="command-badge moderation">moderation</span>
                                    <span class="badge <%= command.type === 'builtin' ? 'badge-builtin' : 'badge-custom' %>">
                                        <%= command.type === 'builtin' ? 'Built-in' : 'Custom' %>
                                    </span>
                                    <span class="command-status <%= command.enabled ? 'enabled' : 'disabled' %>">
                                        <%= command.enabled ? 'Enabled' : 'Disabled' %>
                                    </span>
                                </div>
                                <p class="command-desc"><%= command.description || 'No description' %></p>
                            </div>
                            <div class="command-actions-row">
                                <% if (command.type === 'custom') { %>
                                    <button class="btn-icon" onclick="window.location.href='/commands'" title="Edit in Custom Commands">
                                        ✏️
                                    </button>
                                <% } %>
                                <button class="btn-icon toggle" 
                                        onclick="toggleCommand('<%= command.type %>', '<%= command.type === 'custom' ? command._id : command.name %>')" 
                                        title="Toggle">
                                    <%= command.enabled ? '👁️' : '👁️‍🗨️' %>
                                </button>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        </div>
        <% } %>
        
        <!-- General/Custom Commands -->
        <% if ((commandsByCategory.general && commandsByCategory.general.length > 0) || (commandsByCategory.custom && commandsByCategory.custom.length > 0)) { %>
        <div class="command-section">
            <div class="section-header">
                <h2>📝 Other Commands (<%= (commandsByCategory.general?.length || 0) + (commandsByCategory.custom?.length || 0) %>)</h2>
            </div>
            <div class="commands-list">
                <% [...(commandsByCategory.general || []), ...(commandsByCategory.custom || [])].forEach(command => { %>
                    <div class="command-row <%= command.enabled ? '' : 'disabled' %>">
                        <div class="command-main">
                            <div class="command-info">
                                <div class="command-name-row">
                                    <h3><%= command.name %></h3>
                                    <span class="command-badge general">general</span>
                                    <span class="badge <%= command.type === 'builtin' ? 'badge-builtin' : 'badge-custom' %>">
                                        <%= command.type === 'builtin' ? 'Built-in' : 'Custom' %>
                                    </span>
                                    <span class="command-status <%= command.enabled ? 'enabled' : 'disabled' %>">
                                        <%= command.enabled ? 'Enabled' : 'Disabled' %>
                                    </span>
                                </div>
                                <p class="command-desc"><%= command.description || 'No description' %></p>
                            </div>
                            <div class="command-actions-row">
                                <% if (command.type === 'custom') { %>
                                    <button class="btn-icon" onclick="window.location.href='/commands'" title="Edit in Custom Commands">
                                        ✏️
                                    </button>
                                <% } %>
                                <button class="btn-icon toggle" 
                                        onclick="toggleCommand('<%= command.type %>', '<%= command.type === 'custom' ? command._id : command.name %>')" 
                                        title="Toggle">
                                    <%= command.enabled ? '👁️' : '👁️‍🗨️' %>
                                </button>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        </div>
        <% } %>
    </div>
    
    <script>
        async function toggleCommand(type, identifier) {
            try {
                const url = type === 'builtin' 
                    ? `/commands/toggle-builtin/${identifier}`
                    : `/commands/toggle/${identifier}`;
                
                const response = await fetch(url, { method: 'POST' });
                const data = await response.json();
                
                if (data.success) {
                    location.reload();
                } else {
                    alert('Error: ' + data.error);
                }
            } catch (error) {
                alert('Error: ' + error.message);
            }
        }
    </script>
    
    <style>
        .badge-builtin {
            background: rgba(88, 101, 242, 0.2);
            color: var(--primary);
            padding: 0.25rem 0.75rem;
            border-radius: 6px;
            font-size: 0.7rem;
            font-weight: 700;
            text-transform: uppercase;
        }
        
        .badge-custom {
            background: rgba(153, 170, 181, 0.2);
            color: #99aab5;
            padding: 0.25rem 0.75rem;
            border-radius: 6px;
            font-size: 0.7rem;
            font-weight: 700;
            text-transform: uppercase;
        }
        
        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 2rem;
        }
        
        .command-section {
            margin-bottom: 3rem;
        }
        
        .commands-list {
            display: flex;
            flex-direction: column;
            gap: 0.75rem;
        }
    </style>
</body>
</html>