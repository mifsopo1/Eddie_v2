<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Server Protection - Discord Bot Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --sidebar-width: 250px;
            --primary: #5865f2;
            --success: #57f287;
            --danger: #ed4245;
            --warning: #fee75c;
            --dark-bg: #36393f;
            --darker-bg: #2f3136;
            --darkest-bg: #202225;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: var(--dark-bg);
            color: #dcddde;
            margin: 0;
            padding: 0;
        }

        .sidebar {
            width: var(--sidebar-width);
            height: 100vh;
            position: fixed;
            left: 0;
            top: 0;
            background: var(--darker-bg);
            overflow-y: auto;
            z-index: 1000;
        }

        .content {
            margin-left: var(--sidebar-width);
            padding: 30px;
            min-height: 100vh;
        }

        .page-header {
            margin-bottom: 30px;
        }

        .page-title {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .page-subtitle {
            color: #b9bbbe;
            font-size: 1rem;
        }

        .protection-card {
            background: var(--darker-bg);
            border: 2px solid var(--darkest-bg);
            border-radius: 12px;
            padding: 25px;
            margin-bottom: 25px;
            transition: all 0.3s ease;
        }

        .protection-card.enabled {
            border-color: var(--success);
            box-shadow: 0 0 20px rgba(87, 242, 135, 0.2);
        }

        .card-header-custom {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 15px;
            border-bottom: 2px solid var(--darkest-bg);
        }

        .card-title-section {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .icon-box {
            width: 50px;
            height: 50px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
        }

        .icon-spam { background: rgba(237, 66, 69, 0.2); color: var(--danger); }
        .icon-mention { background: rgba(254, 231, 92, 0.2); color: var(--warning); }
        .icon-raid { background: rgba(88, 101, 242, 0.2); color: var(--primary); }

        .card-title-text h3 {
            margin: 0;
            font-size: 1.4rem;
            font-weight: 600;
        }

        .card-title-text p {
            margin: 0;
            font-size: 0.9rem;
            color: #72767d;
        }

        .toggle-switch {
            position: relative;
            width: 60px;
            height: 32px;
        }

        .toggle-switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }

        .toggle-slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: #4e5058;
            transition: 0.3s;
            border-radius: 32px;
        }

        .toggle-slider:before {
            position: absolute;
            content: "";
            height: 24px;
            width: 24px;
            left: 4px;
            bottom: 4px;
            background-color: white;
            transition: 0.3s;
            border-radius: 50%;
        }

        input:checked + .toggle-slider {
            background-color: var(--success);
        }

        input:checked + .toggle-slider:before {
            transform: translateX(28px);
        }

        .settings-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .form-group label {
            display: block;
            font-weight: 600;
            margin-bottom: 8px;
            color: #b9bbbe;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .form-control, .form-select {
            width: 100%;
            padding: 12px 15px;
            background: var(--darkest-bg);
            border: 2px solid var(--darkest-bg);
            border-radius: 8px;
            color: #dcddde;
            font-size: 1rem;
            transition: all 0.2s;
        }

        .form-control:focus, .form-select:focus {
            outline: none;
            border-color: var(--primary);
            background: var(--darkest-bg);
            color: #dcddde;
        }

        .help-text {
            font-size: 0.85rem;
            color: #72767d;
            margin-top: 5px;
        }

        .alert-warning-custom {
            background: rgba(254, 231, 92, 0.1);
            border: 1px solid rgba(254, 231, 92, 0.3);
            border-radius: 8px;
            padding: 15px;
            margin-top: 20px;
            color: var(--warning);
        }

        .save-section {
            position: sticky;
            bottom: 20px;
            background: var(--darker-bg);
            padding: 20px;
            border-radius: 12px;
            border: 2px solid var(--darkest-bg);
            display: flex;
            justify-content: flex-end;
            margin-top: 30px;
        }

        .btn-save {
            background: var(--primary);
            color: white;
            border: none;
            padding: 15px 40px;
            font-size: 1.1rem;
            font-weight: 600;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .btn-save:hover {
            background: #4752c4;
            transform: translateY(-2px);
            box-shadow: 0 5px 20px rgba(88, 101, 242, 0.4);
        }

        .btn-save:disabled {
            background: #4e5058;
            cursor: not-allowed;
            transform: none;
        }

        .btn-save.success {
            background: var(--success);
        }
    </style>
</head>
<body>
    <%- include('partials/sidebar', { page: 'protection' }) %>

    <div class="content">
        <div class="page-header">
            <div class="page-title">
                <i class="fas fa-shield-alt" style="color: var(--primary);"></i>
                Server Protection Settings
            </div>
            <p class="page-subtitle">Configure automated protection against spam, mass mentions, and raid attacks</p>
        </div>

        <!-- Anti-Spam Protection -->
        <div class="protection-card" id="antiSpamSection">
            <div class="card-header-custom">
                <div class="card-title-section">
                    <div class="icon-box icon-spam">
                        <i class="fas fa-ban"></i>
                    </div>
                    <div class="card-title-text">
                        <h3>Anti-Spam Protection</h3>
                        <p>Detect and prevent rapid message spam</p>
                    </div>
                </div>
                <label class="toggle-switch">
                    <input type="checkbox" id="antiSpamEnabled">
                    <span class="toggle-slider"></span>
                </label>
            </div>

            <div class="settings-grid">
                <div class="form-group">
                    <label>Max Messages</label>
                    <input type="number" class="form-control" id="maxMessages" min="3" max="20" value="5">
                    <small class="help-text">Messages allowed in time window</small>
                </div>
                <div class="form-group">
                    <label>Time Window (seconds)</label>
                    <input type="number" class="form-control" id="timeWindow" min="1" max="60" value="5">
                    <small class="help-text">Time period to check for spam</small>
                </div>
                <div class="form-group">
                    <label>Action on Detection</label>
                    <select class="form-select" id="spamAction">
                        <option value="delete">Delete Message</option>
                        <option value="timeout">Timeout User (1 min)</option>
                        <option value="kick">Kick User</option>
                    </select>
                    <small class="help-text">Action taken when spam detected</small>
                </div>
            </div>
        </div>

        <!-- Mass Mention Protection -->
        <div class="protection-card" id="massMentionSection">
            <div class="card-header-custom">
                <div class="card-title-section">
                    <div class="icon-box icon-mention">
                        <i class="fas fa-at"></i>
                    </div>
                    <div class="card-title-text">
                        <h3>Mass Mention Protection</h3>
                        <p>Block messages with excessive mentions</p>
                    </div>
                </div>
                <label class="toggle-switch">
                    <input type="checkbox" id="massMentionEnabled">
                    <span class="toggle-slider"></span>
                </label>
            </div>

            <div class="settings-grid">
                <div class="form-group">
                    <label>Max Mentions</label>
                    <input type="number" class="form-control" id="maxMentions" min="3" max="20" value="5">
                    <small class="help-text">User/role mentions allowed per message</small>
                </div>
                <div class="form-group">
                    <label>Action on Detection</label>
                    <select class="form-select" id="mentionAction">
                        <option value="delete">Delete Message</option>
                        <option value="timeout">Timeout User (5 min)</option>
                        <option value="kick">Kick User</option>
                    </select>
                    <small class="help-text">Action taken when triggered</small>
                </div>
            </div>
        </div>

        <!-- Anti-Raid Protection -->
        <div class="protection-card" id="antiRaidSection">
            <div class="card-header-custom">
                <div class="card-title-section">
                    <div class="icon-box icon-raid">
                        <i class="fas fa-users-slash"></i>
                    </div>
                    <div class="card-title-text">
                        <h3>Anti-Raid Protection</h3>
                        <p>Respond to mass join attacks automatically</p>
                    </div>
                </div>
                <label class="toggle-switch">
                    <input type="checkbox" id="antiRaidEnabled">
                    <span class="toggle-slider"></span>
                </label>
            </div>

            <div class="settings-grid">
                <div class="form-group">
                    <label>Join Threshold</label>
                    <input type="number" class="form-control" id="joinThreshold" min="3" max="50" value="10">
                    <small class="help-text">Joins to trigger protection</small>
                </div>
                <div class="form-group">
                    <label>Time Window (seconds)</label>
                    <input type="number" class="form-control" id="raidTimeWindow" min="10" max="300" value="60">
                    <small class="help-text">Time period to check for raid</small>
                </div>
                <div class="form-group">
                    <label>Action on Detection</label>
                    <select class="form-select" id="raidAction">
                        <option value="kick">Kick New Members</option>
                        <option value="ban">Ban New Members</option>
                    </select>
                    <small class="help-text">Action taken during raid</small>
                </div>
            </div>

            <div class="alert-warning-custom">
                <i class="fas fa-exclamation-triangle"></i>
                <strong>Warning:</strong> Anti-raid will automatically kick/ban new members when threshold is exceeded. This is aggressive - use with caution!
            </div>
        </div>

        <!-- Save Button -->
        <div class="save-section">
            <button class="btn-save" id="saveProtectionSettings">
                <i class="fas fa-save"></i>
                Save All Settings
            </button>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/protection-settings.js"></script>
</body>
</html>