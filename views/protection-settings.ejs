<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Server Protection Settings - Discord Bot Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --sidebar-width: 250px;
            --primary-color: #5865f2;
            --danger-color: #ed4245;
            --success-color: #57f287;
            --warning-color: #fee75c;
        }

        body {
            font-family: 'Segoe UI', sans-serif;
            background: #36393f;
            color: #dcddde;
        }

        .sidebar {
            width: var(--sidebar-width);
            height: 100vh;
            position: fixed;
            left: 0;
            top: 0;
            background: #2f3136;
            padding: 20px 0;
            overflow-y: auto;
        }

        .content {
            margin-left: var(--sidebar-width);
            padding: 30px;
            min-height: 100vh;
        }

        .card {
            background: #2f3136;
            border: 1px solid #202225;
            border-radius: 8px;
            margin-bottom: 20px;
        }

        .card-header {
            background: #202225;
            border-bottom: 1px solid #202225;
            padding: 15px 20px;
            font-weight: 600;
        }

        .card-body {
            padding: 20px;
        }

        .form-label {
            color: #b9bbbe;
            font-weight: 500;
            margin-bottom: 8px;
        }

        .form-control, .form-select {
            background: #202225;
            border: 1px solid #202225;
            color: #dcddde;
            padding: 10px 15px;
            border-radius: 5px;
        }

        .form-control:focus, .form-select:focus {
            background: #202225;
            color: #dcddde;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 0.2rem rgba(88, 101, 242, 0.25);
        }

        .form-check-input {
            width: 50px;
            height: 25px;
            background-color: #4e5058;
            border: none;
            cursor: pointer;
        }

        .form-check-input:checked {
            background-color: var(--success-color);
        }

        .btn-primary {
            background: var(--primary-color);
            border: none;
            padding: 10px 30px;
            font-weight: 600;
            border-radius: 5px;
        }

        .btn-primary:hover {
            background: #4752c4;
        }

        .protection-section {
            border: 2px solid #202225;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 20px;
        }

        .protection-section.enabled {
            border-color: var(--success-color);
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }

        .section-title {
            font-size: 1.3rem;
            font-weight: 600;
            margin: 0;
        }

        .badge-enabled {
            background: var(--success-color);
            color: #000;
            padding: 5px 12px;
            border-radius: 5px;
            font-weight: 600;
        }

        .badge-disabled {
            background: #4e5058;
            color: #dcddde;
            padding: 5px 12px;
            border-radius: 5px;
            font-weight: 600;
        }

        .setting-row {
            margin-bottom: 20px;
        }

        .help-text {
            font-size: 0.9rem;
            color: #72767d;
            margin-top: 5px;
        }

        .icon-circle {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
            margin-right: 15px;
        }

        .icon-spam { background: rgba(237, 66, 69, 0.2); color: var(--danger-color); }
        .icon-mention { background: rgba(254, 231, 92, 0.2); color: var(--warning-color); }
        .icon-raid { background: rgba(88, 101, 242, 0.2); color: var(--primary-color); }
    </style>
</head>
<body>
    <%- include('partials/sidebar', { page: 'protection' }) %>

    <div class="content">
        <div class="container-fluid">
            <h1 class="mb-4">
                <i class="fas fa-shield-alt"></i> Server Protection Settings
            </h1>
            <p class="text-muted mb-4">Configure automated protection against spam, raids, and abuse</p>

            <!-- Anti-Spam Section -->
            <div class="card">
                <div class="card-body">
                    <div class="protection-section" id="antiSpamSection">
                        <div class="section-header">
                            <div class="d-flex align-items-center">
                                <div class="icon-circle icon-spam">
                                    <i class="fas fa-ban"></i>
                                </div>
                                <div>
                                    <h3 class="section-title">Anti-Spam Protection</h3>
                                    <p class="help-text mb-0">Automatically detect and handle spam messages</p>
                                </div>
                            </div>
                            <div class="form-check form-switch">
                                <input class="form-check-input" type="checkbox" id="antiSpamEnabled">
                                <label class="form-check-label ms-2" for="antiSpamEnabled"></label>
                            </div>
                        </div>

                        <hr style="border-color: #202225;">

                        <div class="row">
                            <div class="col-md-4 setting-row">
                                <label class="form-label">Max Messages</label>
                                <input type="number" class="form-control" id="maxMessages" min="3" max="20" value="5">
                                <small class="help-text">Maximum messages allowed in time window</small>
                            </div>
                            <div class="col-md-4 setting-row">
                                <label class="form-label">Time Window (seconds)</label>
                                <input type="number" class="form-control" id="timeWindow" min="1" max="60" value="5">
                                <small class="help-text">Time period to check for spam</small>
                            </div>
                            <div class="col-md-4 setting-row">
                                <label class="form-label">Action</label>
                                <select class="form-select" id="spamAction">
                                    <option value="delete">Delete Message</option>
                                    <option value="timeout">Timeout User</option>
                                    <option value="kick">Kick User</option>
                                </select>
                                <small class="help-text">Action to take when spam detected</small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Mass Mention Section -->
            <div class="card">
                <div class="card-body">
                    <div class="protection-section" id="massMentionSection">
                        <div class="section-header">
                            <div class="d-flex align-items-center">
                                <div class="icon-circle icon-mention">
                                    <i class="fas fa-at"></i>
                                </div>
                                <div>
                                    <h3 class="section-title">Mass Mention Protection</h3>
                                    <p class="help-text mb-0">Block messages with excessive mentions</p>
                                </div>
                            </div>
                            <div class="form-check form-switch">
                                <input class="form-check-input" type="checkbox" id="massMentionEnabled">
                                <label class="form-check-label ms-2" for="massMentionEnabled"></label>
                            </div>
                        </div>

                        <hr style="border-color: #202225;">

                        <div class="row">
                            <div class="col-md-6 setting-row">
                                <label class="form-label">Max Mentions</label>
                                <input type="number" class="form-control" id="maxMentions" min="3" max="20" value="5">
                                <small class="help-text">Maximum user/role mentions allowed</small>
                            </div>
                            <div class="col-md-6 setting-row">
                                <label class="form-label">Action</label>
                                <select class="form-select" id="mentionAction">
                                    <option value="delete">Delete Message</option>
                                    <option value="timeout">Timeout User</option>
                                    <option value="kick">Kick User</option>
                                </select>
                                <small class="help-text">Action to take when triggered</small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Anti-Raid Section -->
            <div class="card">
                <div class="card-body">
                    <div class="protection-section" id="antiRaidSection">
                        <div class="section-header">
                            <div class="d-flex align-items-center">
                                <div class="icon-circle icon-raid">
                                    <i class="fas fa-users-slash"></i>
                                </div>
                                <div>
                                    <h3 class="section-title">Anti-Raid Protection</h3>
                                    <p class="help-text mb-0">Automatically respond to mass join attacks</p>
                                </div>
                            </div>
                            <div class="form-check form-switch">
                                <input class="form-check-input" type="checkbox" id="antiRaidEnabled">
                                <label class="form-check-label ms-2" for="antiRaidEnabled"></label>
                            </div>
                        </div>

                        <hr style="border-color: #202225;">

                        <div class="row">
                            <div class="col-md-4 setting-row">
                                <label class="form-label">Join Threshold</label>
                                <input type="number" class="form-control" id="joinThreshold" min="3" max="50" value="10">
                                <small class="help-text">Number of joins to trigger protection</small>
                            </div>
                            <div class="col-md-4 setting-row">
                                <label class="form-label">Time Window (seconds)</label>
                                <input type="number" class="form-control" id="raidTimeWindow" min="10" max="300" value="60">
                                <small class="help-text">Time period to check for raid</small>
                            </div>
                            <div class="col-md-4 setting-row">
                                <label class="form-label">Action</label>
                                <select class="form-select" id="raidAction">
                                    <option value="kick">Kick New Members</option>
                                    <option value="ban">Ban New Members</option>
                                </select>
                                <small class="help-text">Action to take during raid</small>
                            </div>
                        </div>

                        <div class="alert alert-warning mt-3" style="background: rgba(254, 231, 92, 0.1); border: 1px solid rgba(254, 231, 92, 0.3); color: var(--warning-color);">
                            <i class="fas fa-exclamation-triangle"></i>
                            <strong>Warning:</strong> Anti-raid will automatically kick/ban new members when threshold is exceeded. Use with caution!
                        </div>
                    </div>
                </div>
            </div>

            <!-- Save Button -->
            <div class="text-end">
                <button class="btn btn-primary btn-lg" id="saveProtectionSettings">
                    <i class="fas fa-save"></i> Save Settings
                </button>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/protection-settings.js"></script>

    <script>
        // Visual feedback for toggles
        document.querySelectorAll('.form-check-input').forEach(toggle => {
            toggle.addEventListener('change', function() {
                const section = this.closest('.protection-section');
                if (this.checked) {
                    section.classList.add('enabled');
                } else {
                    section.classList.remove('enabled');
                }
            });
        });
    </script>
</body>
</html>